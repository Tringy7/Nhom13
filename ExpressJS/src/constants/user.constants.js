const USER_ROLE = Object.freeze({
    ADMIN: 'admin',
    USER: 'user',
    MANAGER: 'manager',
    ADMIN: 'admin'
});

const USER_GENDER = Object.freeze({
    MALE: 'MALE',
    FEMALE: 'FEMALE',
    OTHER: 'OTHER'
});

module.exports = {
    USER_ROLE,
    USER_GENDER
};